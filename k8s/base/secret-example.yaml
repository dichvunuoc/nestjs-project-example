apiVersion: v1
kind: Secret
metadata:
  name: app-secrets
type: Opaque
data:
  # Base64 encoded values
  # DATABASE_URL, etc.
  # This is just an example/template. DO NOT COMMIT REAL SECRETS.
  DB_PASSWORD: "CHANGE_ME_BASE64"
  JWT_SECRET: "CHANGE_ME_BASE64"
